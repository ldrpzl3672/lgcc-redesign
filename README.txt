PUT THESE FILES INTO THE MATCHING LOCATIONS IN YOUR LOCAL REPO, THEN PUSH:

/script.js
/styles.css
/data/upcoming-events.json
/assets/spring-fling-card.png

This package matches your CURRENT live repo structure:
- script.js still reads data.upcoming
- upcoming-events.json still uses the upcoming array
- the card image is added with cardImage
- styles.css is a FULL replacement file with the new featured-card rules appended
